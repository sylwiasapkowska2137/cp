\documentclass[zad,zawodnik]{sinol}
  \title{Robocik}
  \author{Jacek Tomasiewicz}
  \pagestyle{fancy}
  \signature{jtom???}
  \id{rob}
  \iomode{stdin}
  \etap{¶rednia}
  \day{3}
  \date{23.01.2013}
  \RAM{32}
  \history{2011.02.20}{JTom, pomys³ i~redakcja}{1.00}

\begin{document}
  \begin{tasktext}% Ten znak % jest istotny!
Ma³y robocik jest ustawiony na kwadratowej planszy podzielonej na  $n^2$ pól. Robocikowi zosta³a zaprogramowana sekwencja ruchów, któr±  bêdzie ca³y czas powtarza³. Dozwolone s± tylko 4 ruchy: góra  (\texttt{G}), dó³ (\texttt{D}), lewo (\texttt{L}), prawo (\texttt {P}).

Chcieliby¶my znaæ minimaln± liczbê ruchów, które nale¿y do³o¿yæ do  sekwencji ruchów (w dowolne miejsca), tak aby robocik nigdy nie  wyszed³ poza planszê. 
 
  \section{Wej¶cie}
Pierwszy wiersz wej¶cia zawiera cztery liczby ca³kowite $n, m, x, y$  ($2 \leq n \leq 10^9, 1 \leq x, y \leq n, 1 \leq m \leq 10^6$), oznaczaj±ce  odpowiednio wielko¶æ planszy, liczbê ruchów w zaprogramowanej sekwencji oraz wspó³rzêdne po³o¿enia robocika. Robocik stoi w kolumnie $x$ (licz±c od lewej strony) oraz w wierszu $y$ (licz±c od do³u).

Kolejny wiersz zawiera sekwencjê ruchów w postaci s³owa z³o¿onego z  $m$ znaków: \texttt{G}, \texttt{D}, \texttt{L}, \texttt{P}.

  \section{Wyj¶cie}
Pierwszy i jedyny wiersz wyj¶cia powinien zawieraæ jedn± liczbê  ca³kowit±, równ± minimalnej liczbie ruchów, które nale¿y do³o¿yæ do  sekwencji ruchów, tak aby robocik nigdy nie wyszed³ poza planszê.

	\exampleimg{rob0.eps}
     \makestandardexample

\bigskip
\noindent
\textbf{Wyja¶nienie do przyk³adu:} 
Maj±c sekwencjê ruchów \texttt{LD}, po wykonaniu 4 ruchów, czyli  \texttt{LDLD} robocik wyjdzie poza planszê. Je¶li dodamy dwa ruchy,  otrzymuj±c sekwencjê \texttt{GLDP}, robocik nigdy nie wyjdzie poza  planszê.

  \end{tasktext}
\end{document}
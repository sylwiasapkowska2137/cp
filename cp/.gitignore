 */*         #ignore all files in each directory
 !*/*.cpp      #unignore .c files in each directory
 !*/*.sh     #unignore .h header files in each directory
 !.gitignore #unignore .gitignore
 
 o.sh

#include <bits/stdc++.h>

using namespace std;

const int MAX = 307;
int tab[MAX];

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	int n;
	for (int i = 0; i<n; i++){
		cin >> tab[i];
	}
	
	return 0;
}

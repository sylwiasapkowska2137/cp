#include <bits/stdc++.h>
using namespace std;

#define debug(x) cerr << x << " "

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	string s,t,res;
	cin >> s >> t;
	int n = s.size();
	int m = t.size();
	
	return 0;
}
